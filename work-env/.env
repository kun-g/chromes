# OAuth2 Proxy 配置
# 从你的 OIDC 提供商（如 Google, GitHub, Azure AD 等）获取
OAUTH2_PROXY_CLIENT_ID=your-client-id-here
OAUTH2_PROXY_CLIENT_SECRET=your-client-secret-here

# Cookie 密钥 - 必须是 32 字节的 base64 编码字符串
# 可以使用以下命令生成: openssl rand -base64 32
OAUTH2_PROXY_COOKIE_SECRET=change-this-to-a-random-32-byte-base64-string

# OIDC 发行者 URL
# 例如 Google: https://accounts.google.com
# 例如 GitHub: https://github.com/login/oauth
# 例如 Azure AD: https://login.microsoftonline.com/{tenant-id}/v2.0
OAUTH2_PROXY_OIDC_ISSUER_URL=https://your-oidc-provider.com

# ttyd 密码
TTYD_PASSWORD=changeme